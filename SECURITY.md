# 安全说明（Security Policy）

## 支持范围

- 当前支持版本：kid 0.1.x / admin 0.1.x（Git 主分支）。
- 旧版本仅在有明确安全修复时公告。

## 安全设计摘要

- **远程更新防篡改**：更新清单使用 ECDSA（P-256，SHA256withECDSA）签名，签名验证先于一切元数据校验；APK 下载后校验 SHA-256；防回滚（版本单调）、防重放（时间窗）、强制 HTTPS（`core/update/`）。
- **更新私钥**：`release-keys/update-manifest-private.pem` 离线保管，**绝不提交仓库**；公钥内嵌客户端用于验证。
- **数据隔离**：数据库按字段级安全规则做家庭隔离（`docs/DEPLOY.md` 第 3 节），任何第三方无法跨家庭读取或篡改。
- **权限最小化**：被控端权限仅限管控功能必需（用量统计/无障碍/悬浮窗/安装/通知/电池豁免/自启动/设备管理器），不采集定位、通讯录、短信、相册、硬件标识；无广告与统计 SDK。
- **传输安全**：全部 HTTPS；令牌支持刷新轮换。

## 已知限制（如实声明）

- 拦截依赖无障碍服务与使用情况访问；两项被关闭会降级（管理端会收到异常通知）。
- 防卸载不防 root/刷机（设备管理器机制局限）。
- 系统时间回拨可短暂绕过限时（有检测与异常上报，非绝对防护）。

## 报告漏洞

- 请勿在公开 Issue 中提交涉及真实环境/数据/凭据的细节。
- 报告方式：向仓库维护者发送邮件（见 NOTICE 或仓库主页联系方式），或创建 Issue 并**脱敏**描述。
- 预期响应：5 个工作日内确认，修复后同步公告。

## 环境与凭据提醒

- 仓库中的环境 ID 为占位符 `YOUR_ENV_ID`，真实环境由使用者自建（`docs/DEPLOY.md`）。
- 签名私钥、数据库凭据、keystore 一律不提交；使用 `keystore.properties.example` 与 `cloudbase.local.properties.example` 模板自行配置。
